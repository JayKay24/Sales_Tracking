{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.2"
  },
  "scripts": {
    "build": "lerna run build --parallel",
    "test:ci": "lerna run test:ci",
    "test": "lerna run test --parallel"
  }
}
